<h2><%=@course.title %>'s Students'</h2>
<p> <%= link_to 'Add Student', teacher_add_students_path %></p>
<% @students.each do |student| %>
  <p> <%= student.name %></p>
  <p> <%= link_to 'Un-enroll', teacher_unenroll_student_path(@course.id,student.id) %></p>
<% end %>