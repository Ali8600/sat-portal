<h1>Welcome to Student Portal</h1>

<% if @student_item.any? %>
  <ul>
    <% @student_item.each do |course| %>
      <li><%=course.title %></li>
      <!-- Add more details as needed -->
    <% end %>
  </ul>
<% else %>
  <p>No course found.</p>
<% end %>
