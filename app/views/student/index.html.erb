<h1>Welcome to Student Portal</h1>

<% if @courses.any? %>
  <ul>
    <% @courses.each do |course| %>
      <li><%=course.title %></li>
      <!-- Add more details as needed -->
    <% end %>
  </ul>
<% else %>
  <p>No course found.</p>
<% end %>
